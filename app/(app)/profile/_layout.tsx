import React from "react";
import { Stack } from "expo-router";

const Profile = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    ></Stack>
  );
};

export default Profile;
